# ── Runtime deps for the Gradio app / HF Spaces (free CPU tier) ───────────────
# Kept light on purpose: the demo's Text-to-SQL tool uses the Groq fallback (no GPU),
# and the Evals tab reads precomputed eval_results/*.json. For the fine-tuned adapter
# see requirements-finetune.txt; to RUN the eval suite see requirements-dev.txt.
# Target: Python 3.11.

# Agent orchestration
langgraph==0.2.60
langchain-core==0.3.28
langchain-groq==0.2.1          # orchestrator LLM (Groq, free tier)

# Observability / tracing (optional but light)
langsmith==0.2.4

# Retrieval: Chroma + its built-in ONNX MiniLM embedder (no torch)
chromadb==0.5.23

# Web search tool
duckduckgo-search==6.3.7
tavily-python==0.5.0           # used only if TAVILY_API_KEY is set

# SQL safety parsing
sqlparse==0.5.3

# UI
gradio==5.9.1

# Utilities
python-dotenv==1.0.1
